# Multi-task catalog and app checkpoint — 2026-09-15

Server changes are limited to jsaStandalone plus its isolated catalog helper. Authenticated catalog operation uses the verified company and driver/audience/entitlement checks; it accepts no company override. New client template references validate id, version and content hash; create compares the assembled submitted wording with published content and archives reference/PPE/prepared labels. Existing 46 standalone tests and catalog isolation/selection checks pass; Functions TypeScript passes. No deployment.

Remaining rollout work: company-governed publication enforcement (existing direct-write template rules are permissive), required-job receipt task binding, selecting/reviewing a newly required task during Add Location, immutable retry resolution after catalog changes, atomic publication/catalog read consistency, emulator and device flow tests. Do not enable schemaVersion 2 for Liquid Gold yet. The app deliberately blocks unsupported governed multi-task reads rather than silently substituting another assessment. Keep the existing single-template production path until these gates pass. No live acknowledgements or signatures created.
