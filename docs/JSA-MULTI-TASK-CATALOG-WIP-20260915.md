# Multi-task catalog and app checkpoint — 2026-09-15

Server changes are limited to jsaStandalone plus its isolated catalog helper. Authenticated catalog operation uses the verified company and driver/audience/entitlement checks; it accepts no company override. New client template references validate id, version and content hash; create compares the assembled submitted wording with published content and archives reference/PPE/prepared labels. Existing 46 standalone tests and catalog isolation/selection checks pass; Functions TypeScript passes. No deployment.

Add Location now reviews and archives a newly selected task assessment without changing the original signature (backend 7721b223, app a66c594).

Retry checkpoint: new creates and additions store a canonical submitted-request SHA-256 separately from their existing content hash. The production store reads the existing owned record before consulting the current catalog. Exact retries recover that record without a write; changed payloads conflict. Membership, audience and entitlement are still checked on every retry. The final transaction repeats the identity/hash check for concurrent writers. Records without this metadata retain the previous content-hash validation path; no historical records were migrated. Request object key order is ignored, while array order and all values are retained.

Validation: Functions TypeScript build, 46 standalone server fixture cases, and catalog fixture tests pass. Tests cover create and append retries after retirement, response loss and close, unchanged signature/acknowledgement timestamps, altered payload conflicts, revoked membership, ownership mismatch and rejection of first-time retired references. No live writes, deployment, APK build or installation. Phones remain on vc46.

Remaining rollout work: company-governed publication enforcement (existing direct-write template rules are permissive), required-job receipt task binding, atomic publication/catalog read consistency, emulator and device flow tests including client persistence of retry IDs/payloads. Do not enable schemaVersion 2 for Liquid Gold yet. The app deliberately blocks unsupported governed multi-task reads rather than silently substituting another assessment. Keep the existing single-template production path until these gates pass. No live acknowledgements or signatures created.

Required-job selection foundation: `jsaRequiredTaskAssessment.ts` resolves authoritative task names inside an explicit package, chooses exact assignments before a same-package default, rejects missing/ambiguous matches, and deduplicates tasks sharing a template. Its deterministic assessment hash includes task set, package, and template IDs/versions/content hashes. Fixture tests cover Loading/Unloading separation, shared wording, catalog order, version/content changes and package isolation. Functions build passes.

This resolver is not wired to production receipt handlers yet. Inspection of `jsaReceiptHandlers.ts`/`jsaReceiptCore.ts` shows registration currently carries an opaque client jobRef/groupRef and policy intent, with identity/shift authored by the server; it does not resolve authoritative task requirements. Do not treat client jobType text as authority or add a client-selected assessment hash as proof of the required task. Next integration needs a verified current WB-T job source and task/package mapping, frozen assessment in registration/context, completion validation and archival, plus receiver consumption tests. The inspected `C:/dev/wbt-vc104-laptop-integration/utils/ssoOutbound.ts` still describes receipt registration as a stub, so that checkout is not proof of the current desktop handoff implementation. No WB-T files changed.
