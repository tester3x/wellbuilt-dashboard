rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Wide open for now — tighten after confirming uploads work
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
